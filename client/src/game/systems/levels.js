var Levels = new Class({Extends:System,
  currentLevel:0,
  levels:[],

  setLevel:function(levelIndex)
  {
    currentLevel = index;
  },

	addLevel:function(level)
	{
    
	},

  removeLevel:function(level)
  {

  },

  getCurrentLevel:function()
  {
    return this.levels[this.currentLevel];
  }
});